[package]
name = "xen-theory"
version = "0.1.0"
edition = "2021"

[features]
serde = ["dep:serde"]

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }
num = { version = "0.4.3", features = ["serde"] }
